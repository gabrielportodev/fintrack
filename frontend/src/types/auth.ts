export type TokenType = {
  accessToken: string
}

export type RegisterType = {
  name: string
  email: string
}

export type UserType = {
  id: string
  name: string
  email: string
  createdAt: string
}
